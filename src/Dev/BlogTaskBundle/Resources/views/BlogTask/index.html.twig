{% extends '::base.html.twig' %}

{% block body -%}
    <h1>BlogTask list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Task</th>
                <th>Title</th>
                <th>Annotation</th>
                <th>Textbody</th>
                <th>Created</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('blogtask_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.task }}</td>
                <td>{{ entity.title }}</td>
                <td>{{ entity.annotation }}</td>
                <td>{{ entity.textbody }}</td>
                <td>{% if entity.created %}{{ entity.created|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('blogtask_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('blogtask_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('blogtask_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
